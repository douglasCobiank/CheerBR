# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# TanStack Router: routeTree e gerado a partir de src/routes/
# Nao commitar o artefato auto-gerado (causa merge noise e stale-tree bugs).
src/routeTree.gen.ts

# Secrets / env
.env
.env.*
!.env.example
.dev.vars

# .NET build artifacts
bin/
obj/
*.user
*.suo
*.Cache
project.lock.json
project.fragment.lock.json
artifacts/

# Uploads (logos) - nunca commitar binarios de upload
**/wwwroot/uploads/*
!**/wwwroot/uploads/.gitkeep

# Postgres data volume (local dev)
.pgdata/

# Seed data corrompido/legacy: frontend-only snapshot, sem imports no codigo.
# Source of truth hoje e o PostgreSQL. Manter apenas como referencia local.
src/data/teams.seed.json
